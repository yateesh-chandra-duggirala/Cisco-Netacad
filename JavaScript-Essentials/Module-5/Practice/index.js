function getElement(elements, index) {
    return elements[index];
}
    
let elements_array = ["Priya", "Jyosthna", "Radha", "Maheswari", "Uma"];
let result = getElement(elements_array, 3)
console.log(result);
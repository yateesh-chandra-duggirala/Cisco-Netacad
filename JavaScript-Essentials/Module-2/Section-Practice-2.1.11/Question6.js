// Try to modify the line const str1 = 42 + "1"; to get the result 43 (without removing the quotes around 1).

const str1 = 42 + "1"; 
console.log(str1);

// Changing in such a way that 43 will be respective output.
const str2 = 42 + + "1";
console.log(str2);
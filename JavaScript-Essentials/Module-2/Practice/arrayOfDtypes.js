let female_names = [['Kaveri', 'Godavari', 'Narmada', 'Tapathi'], ['Indian', 'Bay of Bengal', 'Arabian']]

// Logging Array List.
console.log(female_names);

// Index printing second array.
console.log(female_names[1]);

// printing the element at index inside another index
console.log(female_names[0][3]);

// printing the length of the list.
console.log(female_names[0].length);
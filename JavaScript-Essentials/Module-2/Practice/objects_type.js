// Let us declare a variable that stores an empty object
let var_obj = {}    // {} indicates that is an object

// Printing var_obj value
console.log(var_obj)

// Printing the type of var_obj value
console.log(typeof(var_obj))

// let  us create an object with fields
// Key : VALUE pairs
let testObj = {
    name : "Yateesh",
    age : 24
};

console.log(testObj.name);
console.log(testObj.age);
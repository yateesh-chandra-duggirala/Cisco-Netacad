// Rewrite the code from the Exercise-1, replacing if with a ternary conditional operator.
let input = prompt("Enter a number :");
let message = (input > 90 && input < 110) ? "Bingo!" : "Miss";
alert(message);